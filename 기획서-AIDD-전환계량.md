# yield-audit v0.3 — AIDD 전환 계량 (Phantom Productivity 렌즈)

- **작성일**: 2026-09-05
- **목적**: "AI 대전환(AIDD)을 했는데 실제로 성과가 올랐는가?"를 코드량·커밋 수가 아닌 **아웃컴**(리워크율, 검증 비용, 사망 컨텍스트)으로 계량하는 yield-audit의 확장 기획
- **시장 근거**: [SDLC AI Radar 2026](https://www.ltm.com/insights/reports/sdlc-ai-radar-2026)이 "phantom productivity" — velocity 지표는 AI가 부풋는데 실제 성과는 아님 — 를 공식 리스크로 명시. [Market Reality Check](https://guptadeepak.com/ai-agent-observability-evaluation-governance-the-2026-market-reality-check/)도 "배포 속도 > 관리 툴링 속도"의 5개 갭 중 계량·거버넌스 축이 미점유. **"AI 기여 커밋 vs 인간 커밋의 아웃컨 차이"를 로컬에서 재는 오픈소스는 확인상 없음** — 빈 땅 등급 ⭐⭐⭐ (기존 자산의 직접 확장이므로 실행 위험 최저)

---

## 한 줄 정의

> CI 대시보드는 "AI 도입 후 커밋 40% 증가"라고 말한다. yield-audit는 "그 중 22%가 2주 내 리워크됐고, 리워크율은 AI 커밋이 인간 커밋의 1.7배"라고 말한다.

---

## 문제 정의: 업계가 공식화한 phantom productivity

1. **LOC·커밋·PR 수는 AI 시대에 무의료해짐** — 에이전트는 한 번에 수백 줄을 쓰고 버린다. 생성량 ≠ 성과
2. **도입 결정권자(CTO/조직장)가 판단 근거를 가질 도구가 없음** — 벤더 대시보드는 자사 도구 사용량만 보여줌(이해충돌).中立 계량 도구 부재
3. **기존 yield-audit는 세션 단위 세금 계량** — M1~M10은 "에이전트 세션 안에서 무엇이 낭비됐나". 아직 "그 세션이 만든 산출물이 이후 어떻게 됐나"(git 아웃컴)와 연결 안 됨
4. **AIDD 전환 조직의 실제 질문은 비교 실험** — "AI 쓰는 팀 vs 안 쓰는 팀", "도입 전 vs 후", "Copilot vs Claude Code" — 이 질문에 답하려면 세션 로그와 git 히스토리의 **조인 키**가 필요하다

---

## 핵심 설계: 세션 ↔ 커밋 조인

기존 렌즈가 트랜스크립트(Claude Code/Codex JSONL)만 보았다면, v0.3은 **트랜스크립트 × git 아웃컴** 이중 소스. 조인 경로:

```
세션 로그 (기존)                     git 아웃컴 (신규)
─────────────────                   ─────────────────
session id ──┐                      commit hash
  timestamp ─┼── 시간창 + author/co-author 매칭 ──→ commit hash
  cwd/project┘                      이후 히스토리: revert? hotfix? churn?
```

- **1차 조인: 시간창 + 경로** — 세션 종료 후 N시간 내, 같은 프로젝트 경로의 커밋
- **2차 정제: Co-authored-by / 툴 흔적** — `Co-Authored-By: Claude`, `🤖 Generated with` 등 푸터 표지. 표지 없는 커밋은 시간창 매칭만으로 "probable_ai" 등급 부여 (확률이 아니라 매칭 근거 라벨: `certain | probable | human`)
- **규약 준수**: measurement 라벨(결정적 근거 표기)은 기존 렌즈 계약 그대로. 추정이 섞이면 라벨로 밝힘 — coroner의 `suspect_commit`과 같은 "증거≠판결" 원칙

---

## 신규 렌즈 (M 시리즈 확장)

| 렌즈 | 질문 | 측정(결정적) |
|---|---|---|
| **M11 AI 리워크율** | AI 커밋은 얼마나 빨리 다시 수정되나 | 커밋 후 D일 내 같은 파일/라인 수정(revert, hotfix, refactor) 비율. AI vs human 코호트 비교 |
| **M12 정착률(survival)** | AI가 쓴 코드는 몇 달 뒤에도 살아있나 | 커밋 라인의 N주 후 생존율(라인 단위 git blame 스냅샷. tombstone과 정반대 방향의 같은 기술) |
| **M13 검증 세금 전이** | 생성은 저렴해졌는데 검증 비용은? | AI 커밋 직후 CI 실행 수, 리뷰 왕복 횟수(커밋-리뷰-수정 사이클 길이) |
| **M14 버스 여부(라인 생존 상세)** | AI 코드가 사고 지점인가 | 인시던트 수정 커밋(revert/fix)이 지적한 라인의 출처 커밋 근거 라벨 분포 |

우선순위: **M11 → M12** (M13/M14는 외부 데이터(CI API) 의존이라 v0.4+). M11만으로도 "AI 리워크율 1.7배"라는 한 문장 세일즈가 성립.

---

## 워크플로

| 단계 | LLM | 내용 |
|---|---|---|
| 수집 | 불필요 | 기존: 트랜스크립트. 신규: `git log`/`blame` 정기 스냅샷 (순수 로컬) |
| 조인 | 불필요 | 시간창+경로 매칭, 푸터 표지 정규식, 근거 라벨 부여 |
| 계량 | 불필요 | 렌즈 순수 함수 (기존 계약 동일) |
| 리포트 | 불필요 | `yield-audit aidd --before 2026-03 --after 2026-09`: 코호트 비교 표. LLM 불요 |
| MCP 조회 | 옵션 | 기존 대로 — 조직장이 "이번 달 AI 리워크율?"을 에이전트에게 질의 |

---

## 제품 원칙 (기존 문법 계승 + 신규)

- 기존 5원칙(로컬 기본, 결정적 렌즈, measurement 라벨, 무망상 파이프라인, P0–P6 클레임 게이트) 그대로
- **추가: 코호트 라벨은 근거 기반** — "이 커밋이 AI 것" 판정은 `certain`(푸터 표지) / `probable`(시간창) / `human` 세 등급만. 백분율 클레임엔 항상 코호트 근거 분포 동봉
- **추가: 판정 아님 원칙** — "AI 코드가 나쁘다"가 아니라 "이 조직에서 이 기간엔 이런 차이가 있었다". AIDD 찬반 도구가 아니라 계량 도구

---

## 경쟁·차별

- **DX / Jellyfish / LinearB** 등 엔지니어링 인텔리전스 SaaS: 엔터프라이즈 계약, 데이터가 클라우드로, AI 벤더와 제휴 이해충돌. yield-audit는 로컬·무료·중립
- **ccusage류**: 사용량·비용 집계만, 아웃컴 없음
- **Langfuse/Phoenix**: 앱 레이어 LLM 관측 — 개발 워크플로 아웃컴과 무관
- 진입장벽이 낮은 만큼 **"트랜스크립트×git 조인 + 아웃컴 렌즈" 조합의 선점**이 전부. HANDOFF의 v0.2(어댑터 인터페이스)가 완성된 지금이 절호의 타이밍 — 어댑터가 벤더 확장(claude/codex/+)을 이미 지원하므로 코호트 비교가 자연스럽다

---

## 마일스톤

- **v0.3.0**: M11 리워크율 (커밋 표지 정규식 + 시간창 조인 + D일 리워크 측정). E2E 골든 테스트: 픽스처 git 저장소에 AI/human 혼합 커밋
- **v0.3.1**: M12 생존율 (blame 스냅샷 — `--snapshot` 서브커맨드로 정기 실행 가정)
- **v0.4**: `aidd` 서브커맨드 통합 리포트, M13(CI 데이터 — GH Actions API 선택적)
- 선행 조건: HANDOFF P0(원격 저장소 + 첫 CI)가 먼저. 이 기획서는 v0.2 어댑터 완료 직후의 다음 사이클 후보로 우선순위 경합(M7/M9/M10과). **권고: M11을 M7보다 먼저** — 시장 언어(phantom productivity)와 맞물린 지금이 윈도우
